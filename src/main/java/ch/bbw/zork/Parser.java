package ch.bbw.zork;/* * author:  Michael Kolling, Version: 1.0, Date: July 1999 * refactoring: Rinaldo Lanza, September 2020 */import ch.bbw.zork.Items.Item;import ch.bbw.zork.enums.Direction;import java.io.BufferedReader;import java.io.IOException;import java.io.InputStream;import java.io.InputStreamReader;import java.util.InputMismatchException;import java.util.Objects;import static ch.bbw.zork.Constants.*;public class Parser {	private InputStream inputStream;    private Player player;	public Parser(InputStream inputStream) {		this.inputStream = inputStream;	}	public String[] getCommandInputs() {		BufferedReader bufferedReader = new BufferedReader(new InputStreamReader(this.inputStream));		try {            System.out.print("> ");			String inputLine = bufferedReader.readLine();            return inputLine.split(" ");		} catch (Exception exc) {			System.out.println("There was an error during reading: %s");            System.err.println(exc.getMessage());		}		return null;	}    public static void clearScreen() {        System.out.print("\033[H\033[2J");        System.out.flush();    }    public String promptInput(String prompt) {        BufferedReader bufferedReader = new BufferedReader(new InputStreamReader(this.inputStream));        String input = "";        while (input.isEmpty()) {            try {                System.out.print(prompt);                input = bufferedReader.readLine();            } catch (IOException exc) {                System.out.println("You cannot have no name!");            }        }        return input;    }    public int chooseOption(String prompt, String[] options) {        BufferedReader bufferedReader = new BufferedReader(new InputStreamReader(this.inputStream));        int optionCount = options.length;        for (int i = 0; i < optionCount; i++) {            System.out.println((i + 1) + ". " + options[i]);        }        while (true) {            try {                System.out.print(prompt);                String input = bufferedReader.readLine();                int choice = Integer.parseInt(input);                if (choice > optionCount || choice < 0) {                    throw new InputMismatchException("Invalid choice.");                }                return choice;            }            catch (InputMismatchException e) {                System.out.println("Invalid choice. Please enter a number between 0 and " + optionCount);            }            catch(Exception exc) {                System.out.println("There was an error during reading: " + exc.getMessage());            }        }    }    public void processCommand(String[] inputs) {        clearScreen();        switch (inputs[0]) {            case "move":                this.move(inputs[1]);                Game.addTime(-MOVE_TIME);                break;            case "scan":                this.scan();                Game.addTime(-SCAN_TIME);                break;            case "check":                this.check(inputs[1]);                Game.addTime(-CHECK_TIME);                break;            case "take":                this.take(inputs[1]);                Game.addTime(-TAKE_TIME);                break;            case "drop":                this.drop(inputs[1]);                Game.addTime(-DROP_TIME);                break;            case "stash":                this.stash(inputs[1]);                Game.addTime(-STASH_TIME);                break;            case "fetch":                this.fetch(inputs[1]);                Game.addTime(-FETCH_TIME);                break;            case "map":                this.map();                break;            case "inventory":                this.inventory();                break;            case "help":                this.help();                break;            case "quit":                this.quit();                break;            default:                System.out.println();                throw new InputMismatchException("Invalid command.");        }        System.out.println();    }    public void move(String direction) {        Direction dir = Direction.parse(direction);        player.move(dir);    }    public void scan() {        player.scan();        player.printAllUncoveredFurnitureAndItems();    }    public void check(String furnitureID) {        player.check(Integer.parseInt(furnitureID));    }    public void take(String input) {        int itemID = Integer.parseInt(input);        String container = player.take(itemID);        Item item = player.getKnownItem(itemID);        System.out.printf("You took the %s in your %s\n", item.getName(), container);    }    public void drop(String input) {        Item item = player.drop(input);        if (item != null) {            Room currentRoom = Game.getPlayer().getCurrentRoom();            currentRoom.dropItemOnFloor(item);        }    }    public void stash(String input) {        if (!Objects.equals(input, "left") && !Objects.equals(input, "l") && !Objects.equals(input, "right") && !Objects.equals(input, "r")) {            int itemID = Integer.parseInt(input);            String containerName = player.take(itemID);            if (containerName.equals("left") ||  containerName.equals("right")) {                player.stash(containerName);            }        } else {            player.stash(input);        }    }    public void fetch(String input) {        player.fetch(Integer.parseInt(input));    }    public void inventory() {        System.out.println(player.getInventory());    }    public void help() {        // TODO: Implement Method    }    public void quit() {        System.out.println("Are you sure you want to quit? (y/N)");        BufferedReader bufferedReader = new BufferedReader(new InputStreamReader(this.inputStream));        try {            String response = bufferedReader.readLine();            if (response.equals("y") || response.equals("yes")) {                System.out.println("Goodbye!");                System.exit(0);            }        }        catch (IOException e) {            System.err.println(e.getMessage());        }    }    public void map() {        System.out.println(Game.getHouse().getMap());    }    public void setPlayer(Player player) {        this.player = player;    }}